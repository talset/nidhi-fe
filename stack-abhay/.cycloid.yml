# Configuration of the CI pipeline
name: 'Abhay stack'
canonical: 'abhay-stack'
description: 'Demo stack which will deploy ec2 on aws'
keywords:
  - 'aws'
  - 'demo'
technologies:
  - technology: Aws Lambda
    version: n/a
author: 'Cycloid'
image: 'https://raw.githubusercontent.com/cycloid-community-catalog/docs-step-by-step-stack/stacks/icon.png'
type: 'stack'
config:
  # Configuration of the CI pipeline
  pipeline:
    pipeline:
      path: 'pipeline/pipeline.yml'
    variables:
      path: 'pipeline/variables.sample.yml'
  terraform:
    example:
      path: 'terraform/example.tf.sample'
      destination: '($ project $)/terraform/($ environment $)/example.tf'
